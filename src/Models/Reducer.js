export const reducer = (state={},action) =>{
    // if(action.type=="WAT"){
    //     let result = action.payLoad.first + action.payLoad.second;
    //     state = {...state,result:result};
    //     return state;
    // }
    return state;
}